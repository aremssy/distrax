APP_NAME=Rentdo
# Use APP_ENV=local + APP_DEBUG=true only for local development. Never enable
# debug in production: it leaks stack traces and configuration.
APP_ENV=production
APP_KEY=base64:dUjVz/d2S34wgv6JIUiadaCQXK2zInNsNso33OC3BW8=
APP_DEBUG=false
APP_URL=https://distrax.fac.com.ng/

# Comma-separated list of origins allowed to call the API from a browser.
# Example: CORS_ALLOWED_ORIGINS=https://example.com,https://app.example.com
CORS_ALLOWED_ORIGINS="${APP_URL}"

# API token lifetime in minutes (default 43200 = 30 days) and an identifying prefix.
SANCTUM_TOKEN_TTL=43200
SANCTUM_TOKEN_PREFIX=rentdo_

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US
# ISO 3166-1 alpha-2 code preselected in phone dial-code selectors.
APP_DEFAULT_COUNTRY=BD

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=distraxdb
DB_USERNAME=distraxdb
DB_PASSWORD="3c$t8@vwXP#"

SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
# Set to false only when serving over plain HTTP in local development.
SESSION_SECURE_COOKIE=true

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=redis
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# ── Installer / Licensing ──────────────────────────────────────────────────
APP_VERSION=1.0.0
ENVATO_API_URL=https://api.envato.com/v3/market/author/sale
ENVATO_PERSONAL_TOKEN=
# Only set to true for local/demo installs without an Envato token. Leaving
# this unset (or false) makes license verification fail closed.
INSTALLER_ALLOW_MANUAL_ACTIVATION=false

# ── Backups (spatie/laravel-backup) ────────────────────────────────────────
# Optional password to encrypt backup archives (leave blank to disable encryption).
BACKUP_ARCHIVE_PASSWORD=
