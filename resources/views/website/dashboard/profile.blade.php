@extends('website.layouts.dashboard')
@section('title', __('Profile').' | '.config('app.name')) @section('page-title', __('Profile'))
@section('dashboard-content')<div class="mx-auto max-w-3xl space-y-6"><section class="rounded-2xl border border-slate-200 bg-white p-4 sm:p-6"><div class="flex flex-col items-start justify-between gap-2 sm:flex-row sm:items-center sm:gap-4"><div><h2 class="font-bold">{{ __('Identity verification') }}</h2><p class="mt-1 text-sm text-slate-500">{{ __('Status:') }} <span class="font-semibold">{{__(ucfirst($user->verification_status))}}</span></p></div>@if($user->verification_status==='verified')<span class="text-emerald-600">✓ {{ __('Verified') }}</span>@endif</div>@if($user->verification_status!=='verified')<form class="mt-4 flex flex-col gap-3 sm:flex-row" action="{{route('dashboard.profile.verification')}}" method="POST" enctype="multipart/form-data">@csrf<input class="flex-1 rounded-lg border border-slate-200 p-2 text-sm" type="file" name="document" accept=".jpg,.jpeg,.png,.pdf" required><button class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white transition hover:bg-indigo-700">{{ __('Submit ID') }}</button></form><p class="mt-2 text-xs text-slate-500">{{ __('JPG, PNG, or PDF up to 5 MB. Stored privately.') }}</p>@endif</section><form class="space-y-5 rounded-2xl border border-slate-200 bg-white p-6" action="{{route('dashboard.profile.update')}}" method="POST">@csrf @method('PUT')<h2 class="font-bold">{{ __('Personal information') }}</h2><div class="grid gap-5 sm:grid-cols-2">@foreach([['name',__('Full name'),'text'],['email',__('Email'),'email']] as [$name,$label,$type])<label class="text-sm font-medium">{{$label}}<input class="mt-2 w-full rounded-lg border border-slate-200 p-3" type="{{$type}}" name="{{$name}}" value="{{old($name,$user->$name)}}"></label>@endforeach<x-website.phone-input name="phone" label="{{ __('Phone') }}" :value="old('phone',$user->phone)" /><label class="text-sm font-medium">{{ __('Language') }}<input class="mt-2 w-full rounded-lg border border-slate-200 p-3" name="language" value="{{old('language',$user->language)}}"></label><label class="text-sm font-medium">{{ __('Currency') }}<input class="mt-2 w-full rounded-lg border border-slate-200 p-3" name="currency" value="{{old('currency',$user->currency)}}"></label><label class="text-sm font-medium">{{ __('Phone visibility') }}<select class="mt-2 w-full rounded-lg border border-slate-200 p-3" name="phone_visibility">@foreach(['everyone','registered','none'] as $value)<option @selected($user->phone_visibility===$value) value="{{$value}}">{{__(ucfirst($value))}}</option>@endforeach</select></label></div><button class="rounded-lg bg-indigo-600 px-5 py-3 text-sm font-semibold text-white">{{ __('Save profile') }}</button></form></div>@endsection
