@extends('website.layouts.master')
@section('title',$agent->user->name.' | Agents')
@section('content')<main class="min-h-screen bg-slate-50 py-10"><div class="mx-auto max-w-7xl space-y-8 px-4"><header class="flex flex-col gap-5 rounded-2xl border border-slate-200 bg-white p-5 sm:flex-row sm:items-center sm:gap-6 sm:p-7"><img alt="{{$agent->user->name}}" class="h-24 w-24 rounded-full object-cover" src="{{$agent->user->avatar ? asset('storage/'.$agent->user->avatar) : asset('assets/images/website/agent/agent-1.webp')}}"><div><h1 class="text-2xl font-bold sm:text-3xl">{{$agent->user->name}}</h1><p class="mt-1 text-slate-500">{{$agent->title}} @if($agent->agency) &middot; <a class="text-indigo-600 hover:underline" href="{{route('agencies.show',$agent->agency->slug)}}">{{$agent->agency->name}}</a>@endif</p>@if($agent->bio)<p class="mt-4 max-w-2xl leading-7 text-slate-600">{{$agent->bio}}</p>@endif<div class="mt-4 flex flex-wrap gap-4 text-sm">@if($agent->phone)<a class="flex items-center gap-1.5 text-slate-600 hover:text-indigo-600" href="tel:{{$agent->phone}}"><i class="w-4 h-4" data-lucide="phone"></i>{{$agent->phone}}</a>@endif @if($agent->whatsapp)<a class="flex items-center gap-1.5 text-slate-600 hover:text-indigo-600" href="https://wa.me/{{$agent->whatsapp}}" target="_blank" rel="noopener"><i class="w-4 h-4" data-lucide="message-circle"></i>WhatsApp</a>@endif</div></div></header><section><h2 class="mb-4 text-xl font-bold">{{ __('Listings by :name', ['name' => $agent->user->name]) }}</h2><div class="grid gap-5 md:grid-cols-2 lg:grid-cols-4">@forelse($listings as $listing)<x-website.property-card :$listing/>@empty<p>{{ __('No active listings.') }}</p>@endforelse</div></section></div></main>@endsection
